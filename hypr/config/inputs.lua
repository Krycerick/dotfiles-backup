hl.config({
  input = {
    mouse_refocus = false,
    sensitivity = 0.0,            -- Use exact float representation
    accel_profile = "flat",       -- Must be a string wrapped in quotes
    force_no_accel = true,        -- True raw mouse input bypass
  },
})
